# Chromabd相关事宜
1. 在运行之前需要先启动chrombd，`"...\chroma_data"`是chroma_data文件夹路径。启动命令如下
```
chroma run --path "...\chroma_data"
```
2. 关闭chromabd
```
Ctrl + C
```
3. 万一提示`'chroma' 不是内部或外部命令`，说明你还没有安装 Python 的 chromadb 包，先运行以下命令安装
```
pip install chromadb
```

# Tesseract相关事宜
